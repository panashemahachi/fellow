<div class="container"  style="padding-left:15%;padding-right:15%;">
<center><h3 style="display:inline" class="text-center"><%= @artifact.title %><small>
Added by
<% if @artifact.user_id == current_user.id %>
      You!
      <% else %>
      <% adder = User.where(id: @artifact.user_id).first.username %>
      <%= link_to adder, profile_link_path(adder)%>
      <% end %></small></h3></center>

  <hr>
  <div class="cont">
    <% if @artifact.tldr != '' %>
    <center><blockquote>TLDR: <%= @artifact.tldr%></blockquote></center>
    <% end %>
<!--
<div class="row" style="float:right">
    <% @artifact.tag_list.each do |t| %>
           <h6 style="display:inline;"><span class="label label-default"><%= t %></span></h6>
           <% end %></h6>
           </div>
           <br /> -->

   <!-- <p><i>Added by 

      <% if @artifact.user_id == current_user.id %>
      You!
      <% else %>
      <% adder = User.where(id: @artifact.user_id).first.username %>
      <%= link_to adder, profile_link_path(adder)%>
      <% end %>

    </i>
   <% @artifact.tag_list.each do |t| %>
           <span class="label label-default"><%= t %></span>
           <% end %>
           
           <i style="float:right"><%= link_to "View Original", @artifact.link, target: '_blank'%></i>

         </p>


         <br />-->
         <div class="artifact-content">
          <%= raw @artifact.content %>
        </div>
      </div>
    </div>

    <br style="margin-bottom:90px;"/>